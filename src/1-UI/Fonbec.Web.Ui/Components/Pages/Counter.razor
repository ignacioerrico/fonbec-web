@page "/counter"

<PageTitle>Counter</PageTitle>

<MudText Typo="Typo.h2">Counter</MudText>

<MudText Typo="Typo.body1">Current count: @_currentCount</MudText>

<MudButton Variant="Variant.Filled" Color="Color.Primary" OnClick="IncrementCount">Click me</MudButton>

@code {
    private int _currentCount = 0;

    private void IncrementCount() => _currentCount++;
}
