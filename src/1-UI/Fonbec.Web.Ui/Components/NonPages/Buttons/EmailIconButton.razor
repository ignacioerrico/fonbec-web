@if (!string.IsNullOrWhiteSpace(EmailAddress))
{
    <MudIconButton Href="@($"mailto:{EmailAddress}")"
                   Icon="@Icons.Material.Outlined.Email"
                   Size="Size.Small" />
}

@code {
    [Parameter]
    public string? EmailAddress { get; set; }
}